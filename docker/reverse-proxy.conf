server {
  listen 80;
  server_name _;

  location /api/ {
    proxy_pass http://backend:8080;
  }

  # The frontend's Nginx serves static assets and applies the SPA fallback.
  location / {
    proxy_pass http://frontend:80;
  }
}
