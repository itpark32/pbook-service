# Чтение и запись файлов

После урока вы сможете безопасно читать и записывать текстовые файлы.

> **Фокус**
> - Нужно знать: конструкции предыдущего урока,
> - Нужно уметь: безопасно читать и записывать текстовые файлы.
> - Полезно знать: сначала выполните Guided 1, затем перенесите приём в Guided 2.

## 1. Зачем нужен этот приём

Здесь важно увидеть конкретный механизм, а не запомнить готовый ответ.

## Основная идея

Файл читают в нужном формате: целиком, построчно или как поток чисел. Контекст `with` закрывает файл автоматически, а преобразование строки в число стоит выполнять там, где известен формат входа. В уроке «Чтение и запись файлов» это основной рабочий приём. В этой теме это относится к умению: безопасно читать и записывать текстовые файлы.

```python
with open('data.txt', encoding='utf-8') as source:
    print(source.read())
```

## 3. Разбор по шагам

Сначала назовите, что хранит каждая ключевая переменная, и проверьте её значение на небольшом входе. Пример ниже показывает именно рабочий механизм этой темы. В этой теме это относится к умению: безопасно читать и записывать текстовые файлы.

```python
with open('answer.txt', 'w', encoding='utf-8') as target:
    target.write('ready')
```

## 4. Шаблон решения

1. Прочитайте условие и выделите входные данные.
2. Назовите переменную, список или таблицу, где будет храниться текущий результат.
3. Выполните один понятный шаг для каждого элемента или состояния.
4. Отдельно проверьте пустой, минимальный и пограничный случаи. В этой теме это относится к умению: безопасно читать и записывать текстовые файлы.

## 5. Частые ошибки

Проверьте границу условия и минимальный допустимый вход.

## Проверьте себя

1. Какое состояние меняется после одного шага алгоритма?
2. Какой крайний случай может дать другой ответ?
3. Как объяснить полученный ответ без запуска программы? В этой теме это относится к умению: безопасно читать и записывать текстовые файлы.

## Где использовать дальше

В Guided 1 повторите механизм на прямом примере, в Guided 2 примените его к другим данным. Checkpoint требует самостоятельно распознать этот приём.
 В этой теме это относится к умению: безопасно читать и записывать текстовые файлы.