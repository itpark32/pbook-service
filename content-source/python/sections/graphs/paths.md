# Пути и расстояния в невзвешенном графе

После урока вы сможете находить расстояния и восстанавливать пути в невзвешенном графе.

> **Фокус**
> - Нужно знать: конструкции предыдущего урока,
> - Нужно уметь: находить расстояния и восстанавливать пути в невзвешенном графе.
> - Полезно знать: сначала выполните Guided 1, затем перенесите приём в Guided 2.

## 1. Зачем нужен этот приём

Здесь важно увидеть конкретный механизм, а не запомнить готовый ответ.

## Основная идея

В невзвешенном графе BFS идёт слоями от старта. Первая встреча вершины даёт минимальное число рёбер до неё, а массив родителей позволяет затем восстановить сам путь. В уроке «Пути и расстояния в невзвешенном графе» это основной рабочий приём. В этой теме это относится к умению: находить расстояния и восстанавливать пути в невзвешенном графе.

```python
graph = [[1], [0, 2], [1]]
distance = [-1, -1, -1]
distance[0] = 0
queue = [0]
for vertex in queue:
    for neighbour in graph[vertex]:
        if distance[neighbour] == -1:
            distance[neighbour] = distance[vertex] + 1
            queue.append(neighbour)
print(distance)
```

## 3. Разбор по шагам

Сначала назовите, что хранит каждая ключевая переменная, и проверьте её значение на небольшом входе. Пример ниже показывает именно рабочий механизм этой темы. В этой теме это относится к умению: находить расстояния и восстанавливать пути в невзвешенном графе.

```python
parent = [None, 0, 1]
path = []
vertex = 2
while vertex is not None:
    path.append(vertex)
    vertex = parent[vertex]
print(path[::-1])
```

## 4. Шаблон решения

1. Прочитайте условие и выделите входные данные.
2. Назовите переменную, список или таблицу, где будет храниться текущий результат.
3. Выполните один понятный шаг для каждого элемента или состояния.
4. Отдельно проверьте пустой, минимальный и пограничный случаи. В этой теме это относится к умению: находить расстояния и восстанавливать пути в невзвешенном графе.

## 5. Частые ошибки

Проверьте границу условия и минимальный допустимый вход.

## Проверьте себя

1. Какое состояние меняется после одного шага алгоритма?
2. Какой крайний случай может дать другой ответ?
3. Как объяснить полученный ответ без запуска программы? В этой теме это относится к умению: находить расстояния и восстанавливать пути в невзвешенном графе.

## Где использовать дальше

В Guided 1 повторите механизм на прямом примере, в Guided 2 примените его к другим данным. Checkpoint требует самостоятельно распознать этот приём.
 В этой теме это относится к умению: находить расстояния и восстанавливать пути в невзвешенном графе.