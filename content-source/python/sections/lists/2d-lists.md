# Двумерные списки

После урока вы сможете создавать матрицы и обходить строки и столбцы.

> **Фокус**
> - Нужно знать: конструкции предыдущего урока,
> - Нужно уметь: создавать матрицы и обходить строки и столбцы.
> - Полезно знать: сначала выполните Guided 1, затем перенесите приём в Guided 2.

## 1. Зачем нужен этот приём

Здесь важно увидеть конкретный механизм, а не запомнить готовый ответ.

## Основная идея

Матрица — список строк. Внешний проход выбирает строку, внутренний — элемент; координаты нужны для столбцов и диагоналей, а прямоугольная матрица не обязана быть квадратной. В уроке «Двумерные списки» это основной рабочий приём. В этой теме это относится к умению: создавать матрицы и обходить строки и столбцы.

```python
matrix = [[2, 5], [7, 1]]
for row in matrix:
    print(sum(row))
```

## 3. Разбор по шагам

Сначала назовите, что хранит каждая ключевая переменная, и проверьте её значение на небольшом входе. Пример ниже показывает именно рабочий механизм этой темы. В этой теме это относится к умению: создавать матрицы и обходить строки и столбцы.

```python
matrix = [[2, 5], [7, 1]]
print(matrix[0][0] + matrix[1][1])
```

## 4. Шаблон решения

1. Прочитайте условие и выделите входные данные.
2. Назовите переменную, список или таблицу, где будет храниться текущий результат.
3. Выполните один понятный шаг для каждого элемента или состояния.
4. Отдельно проверьте пустой, минимальный и пограничный случаи. В этой теме это относится к умению: создавать матрицы и обходить строки и столбцы.

## 5. Частые ошибки

Проверьте границу условия и минимальный допустимый вход.

## Проверьте себя

1. Какое состояние меняется после одного шага алгоритма?
2. Какой крайний случай может дать другой ответ?
3. Как объяснить полученный ответ без запуска программы? В этой теме это относится к умению: создавать матрицы и обходить строки и столбцы.

## Где использовать дальше

В Guided 1 повторите механизм на прямом примере, в Guided 2 примените его к другим данным. Checkpoint требует самостоятельно распознать этот приём.
 В этой теме это относится к умению: создавать матрицы и обходить строки и столбцы.