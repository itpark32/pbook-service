# Изменение списков

После урока вы сможете изменять элементы и осознанно применять методы списка.

> **Фокус**
> - Нужно знать: конструкции предыдущего урока,
> - Нужно уметь: изменять элементы и осознанно применять методы списка.
> - Полезно знать: сначала выполните Guided 1, затем перенесите приём в Guided 2.

## 1. Зачем нужен этот приём

Здесь важно увидеть конкретный механизм, а не запомнить готовый ответ.

## Основная идея

Список хранит упорядоченные изменяемые значения. Выбирайте проход по значениям, когда индекс не нужен, и проход по индексам, когда сравниваете соседей или меняете конкретную позицию. В уроке «Изменение списков» это основной рабочий приём. В этой теме это относится к умению: изменять элементы и осознанно применять методы списка.

```python
values = [4, 1, 7]
values.append(3)
print(values)
```

## 3. Разбор по шагам

Сначала назовите, что хранит каждая ключевая переменная, и проверьте её значение на небольшом входе. Пример ниже показывает именно рабочий механизм этой темы. В этой теме это относится к умению: изменять элементы и осознанно применять методы списка.

```python
values = [4, 1, 7]
print(sum(values))
```

## 4. Шаблон решения

1. Прочитайте условие и выделите входные данные.
2. Назовите переменную, список или таблицу, где будет храниться текущий результат.
3. Выполните один понятный шаг для каждого элемента или состояния.
4. Отдельно проверьте пустой, минимальный и пограничный случаи. В этой теме это относится к умению: изменять элементы и осознанно применять методы списка.

## 5. Частые ошибки

Проверьте границу условия и минимальный допустимый вход.

## Проверьте себя

1. Какое состояние меняется после одного шага алгоритма?
2. Какой крайний случай может дать другой ответ?
3. Как объяснить полученный ответ без запуска программы? В этой теме это относится к умению: изменять элементы и осознанно применять методы списка.

## Где использовать дальше

В Guided 1 повторите механизм на прямом примере, в Guided 2 примените его к другим данным. Checkpoint требует самостоятельно распознать этот приём.
 В этой теме это относится к умению: изменять элементы и осознанно применять методы списка.