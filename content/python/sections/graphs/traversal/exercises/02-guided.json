{
  "schemaVersion": 1,
  "id": "python-graphs-traversal-guided-2",
  "owner": {
    "type": "lesson",
    "id": "python-graphs-traversal"
  },
  "kind": "guided",
  "title": "Достижимые вершины",
  "statement": "Верните отсортированные вершины, достижимые из start в неориентированном графе.",
  "difficulty": "standard",
  "starterCode": "def reachable(...):\n    pass\n",
  "hints": [
    "Сначала сформулируйте, что функция должна вернуть.",
    "Проверьте пустые и граничные данные."
  ],
  "solution": "def reachable(n, edges, start):\n    graph = [[] for _ in range(n)]\n    for left, right in edges:\n        graph[left].append(right)\n        graph[right].append(left)\n    seen = {start}\n    stack = [start]\n    while stack:\n        vertex = stack.pop()\n        for neighbour in graph[vertex]:\n            if neighbour not in seen:\n                seen.add(neighbour)\n                stack.append(neighbour)\n    return sorted(seen)",
  "examples": [
    {
      "input": "[5,[[0,1],[1,2],[3,4]],0]",
      "output": "[0,1,2]"
    }
  ],
  "judge": {
    "type": "function",
    "functionName": "reachable",
    "tests": [
      {
        "args": [
          5,
          [
            [
              0,
              1
            ],
            [
              1,
              2
            ],
            [
              3,
              4
            ]
          ],
          0
        ],
        "expectedReturn": [
          0,
          1,
          2
        ]
      },
      {
        "args": [
          1,
          [],
          0
        ],
        "expectedReturn": [
          0
        ]
      }
    ]
  }
}
